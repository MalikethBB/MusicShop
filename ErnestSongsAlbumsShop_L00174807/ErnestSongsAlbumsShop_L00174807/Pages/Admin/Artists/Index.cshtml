@page
@model ErnestSongsAlbumsShop_L00174807.Pages.Admin.Artists.IndexModel
@{
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h3 class="text-primary">Artists</h3>
        </div>
        <div class="col-6">
            <a asp-page="Create" class="btn btn-primary">
                Create new Artist
            </a>
        </div>
    </div>
</div>

<br /> <br /> <br />

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Artists</th>
            <th>Songs</th>
            <th>Country of Origin</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model.Artists)
        {
            <tr>
                <td width="30%">@obj.Name</td>
                <td width="40%">
                    @if (obj.Songs != null && obj.Songs.Any())
                    {
                        <ul>
                            @foreach (var song in obj.Songs)
                            {
                                <li>@song.Name</li>
                            }
                        </ul>
                    }
                    else
                    {
                        <p>No songs.</p>
                    }
                </td>
                <td width="20%">@obj.Country</td>
                <td> <a asp-page="Edit" asp-route-id="@obj.Id">Edit</a> </td>
                <td> <a asp-page="Delete" asp-route-id="@obj.Id">Delete</a> </td>
            </tr>
        }
    </tbody>
</table>